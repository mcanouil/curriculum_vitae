language: R
cache: packages

pandoc_version: 2.3.1

script:
  - Rscript -e 'rmarkdown::render(input = "curriculum_vitae/curriculum_vitae.Rmd", output_dir = ".", output_file = "cv.html", encoding = "UTF-8")'
  - Rscript -e 'rmarkdown::render_site()'

deploy:
  provider: script
  script: curl -u $FTP_USER:$FTP_PASSWORD -T cv.html ${FTP_URL}index.html
  skip_cleanup: true

#  - provider: script
#    script: find -name '*.html' -type f -exec curl -u $FTP_USER:$FTP_PASSWORD -T {} ${FTP_URL}dev/ \;
#    skip_cleanup: true

notifications:
  email:
    on_success: change
    on_failure: change
